// Package tycho contains new runtime components that can be compiled and
// wired alongside the existing Kepler codebase without changing public pkg/* APIs.
// Everything here is internal to this module while we iterate.
package tycho
