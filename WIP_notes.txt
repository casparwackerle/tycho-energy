installing goplantuml to generate uml diagrams
    did not currently run right

installing go-callvis
    go install github.com/ofabry/go-callvis@latest
    write scripts for easy execution in doc_tycho
    

install gotraceui (target: execution tracing)
    sudo apt install gcc pkg-config libwayland-dev libx11-dev libx11-xcb-dev libxkbcommon-x11-dev libgles2-mesa-dev libegl1-mesa-dev libffi-dev libxcursor-dev libvulkan-dev
    install via unofficial flatpak https://github.com/hdonnay/co.honnef.Gotraceui



start implementation
    notes on config values:
        new config value, do the following (with example)
            new default in types.go
                defaultTychoTrigger            = "redfish"
            new struct in 
                Trigger            string
            add loader in config.go
                Trigger:            getConfig("TYCHO_ANALYSIS_TRIGGER", defaultTychoTrigger),
            add getter in config.go
                func Trigger() string {return instance.TychoAnalysis.Trigger}
            add logging in config.go
                klog.V(5).Infof("TYCHO_ANALYSIS_TRIGGER: %s", instance.TychoAnalysis.Trigger)
            

write normalizeTycho function to protect against bad configurations
    ensure the longest delay is used everythere
    If redish is enabled ensure that the expected publish interval is used to calculate buffer size
    ensure buffer margin is 0 or higher -> at least one full cycle buffer